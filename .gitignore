.DS_Store
__pycache__/
.build/
.xcodebuild/
.xcodebuild-local/
.swiftpm/
.codex/
.harness/
.tmp/
dist/
tokenbar.zip
*.tar.gz
TokenBar.xcodeproj/xcuserdata/
*.xcuserstate

# Design-canvas screenshots — kept on disk for designer reference only;
# excluded from git because filenames contain CJK + spaces that confuse
# downstream tooling (notably ultrareview seed bundle import).
docs/design-prd/tokenbar/uploads/

# Internal-only working docs (date-stamped refactor notes, article drafts) —
# kept on disk for reference, not pushed to the public repo.
docs/refactor-*.md
docs/article-draft.md

# Security audit checklist — local tracking only, never pushed.
SECURITY-FIX-CHECKLIST.md
CACHE-SPLIT-CHECKLIST.md

# Claude Code working state (scheduled-task locks, worktrees etc.) —
# created automatically when claude-code runs against this repo.
.claude/

# Design assets — local reference only, not tracked in git.
design/
build/
